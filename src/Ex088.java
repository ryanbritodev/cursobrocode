public class Ex088 {
    public static void main(String[] args) {

        // Exemplo de uso de Métodos Genéricos
        // Vamos imaginar que estamos criando um jogo e precisamos carregar diferentes sprites

        // Player player = new Player();
        // Enemy enemy = new Enemy();
        // Item item = new Item();
        // Tree tree = new Tree();

        // draw(player);
        // draw(enemy);
        // draw(item);
        // draw(tree);

    }

    public static <Coisa> void draw(Coisa x) {
        // x.draw();
    }
}
